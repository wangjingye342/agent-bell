# AgentBell 设计语言 — TE 铝面板（两端共享契约）

> 适用范围：`tools/agent-bell-bridge/`（Windows 桥接程序设置窗）与 `agent_bell/agent_bell.ino` 内嵌网页控制台。
> 两端是"同一台仪器的两块面板"：控件造型、配色、字重必须同构；平台差异（触屏目标尺寸、DPI 缩放）允许各自适配。

## THESIS

电脑端与设备网页端读起来是同一件硬件的延伸——浅暖灰铝面板、拟物控件、全页唯一深色块是"显示屏"。
拒绝类目默认：暗色 dashboard、圆角卡片堆、彩色粗左边框、eyebrow 标签。

## 令牌（两端一致，十六进制不许漂移）

| 令牌 | 值 | 用途 |
|---|---|---|
| CHASSIS | `#E4E3DF` | 机身/页面底色（浅暖灰铝） |
| LINE | `#C6C5C0` | 分区细线 |
| EDGE_HI | `#F2F1EE` | 机加工内侧高光线（装饰，可选） |
| INK | `#1D1C19` | 主文字 |
| MUTED | `#605F58` | 次要文字（对 CHASSIS 实测 4.99:1；旧 `#8B8A84` 只准用于禁用态/装饰） |
| ORANGE | `#F04E00` | **唯一强调色**：推子填充、拨钮开态、主按钮、焦点框 |
| ORANGE_DK | `#C84100` | 橙按压/描边 |
| KEY_BG / KEY_HI / KEY_DN | `#F6F5F2` / `#FFFFFF` / `#DDDCD7` | 键帽面 / 悬停 / 按下 |
| KEY_EDGE | `#AFAEA8` | 键帽描边 |
| DISP_BG / DISP_EDGE | `#141412` / `#2A2A26` | 显示屏底（全页唯一深色块）/ 屏框 |
| DISP_FG / DISP_DIM | `#EDEBE3` / `#807E75` | 屏内主字 / 暗字 |
| GREEN / RED / AMBER | `#3FB950` / `#E5484D` / `#E8A33D` | 在线 / 离线错误 / 搜索警告 |

## 控件语言

- **区块标题**：大写小字号拉字距标签 + 右侧延伸 1px 细线（LINE）。
- **推子 fader**：2px 轨道 `#B3B2AC` + 橙已填充段 + 竖向滑块帽（KEY_BG 面、3 条防滑纹 `#B0AFA9`）+ 等宽数值。
- **拨钮 toggle**：圆角胶囊轨道（关 `#B7B6B0`，开 ORANGE）+ 白圆滑块。
- **选择器**：雕刻值格显示当前项 + 右侧下拉小箭头，**点击展开全部选项直接选**（桌面=自绘键帽弹层，橙点标当前项，靠底自动向上翻；网页=原生 `select` 样式化，触屏弹系统选择器）。桌面另保留 `‹ ›` 键帽做相邻微调；纯数值参数（轮询/冷却）仍用 `‹ 值 ›` 步进。
- **按钮**：键帽——KEY_BG 面、1px KEY_EDGE 描边、圆角 4–6px、按下整体下沉 1px；主按钮=深橙面（ORANGE_DK，白字 4.99:1 过 AA）白粗体字，悬停亮至 ORANGE。
- **显示屏**：深色圆角块 + 等宽字 + 状态色；网页端保留细扫描线质感（克制）。例外：toast 瞬态提示允许复用屏色（读作"迷你屏"），不算第二个深色块。

## 字体

- 网页端：`system-ui` 正文；`ui-monospace` 标签/数值。
- 桌面端：Microsoft YaHei UI / Segoe UI 正文；Consolas 等宽。

## 形态

- 桌面端：无边框固定尺寸横向面板（pywebview/WebView2 渲染 `tools/agent-bell-bridge/ui/index.html`，逻辑 1120×660，DPI 由 WebView2 自适配），自绘标题栏（字标 + 铭牌 + 状态 LED + – 最小化 + × 键帽收托盘，整条可拖动）、四角装饰螺丝、Win11 DWM 圆角机身。
- 网页端：max-width 480px 手机优先；顶部字标 + `console-1` 铭牌 + 状态 LED；OLED 仿真屏为视觉锚点；触屏目标 ≥44px。

## 措辞对照（两端必须逐字一致）

| 含义 | 定稿措辞 |
|---|---|
| 通知点亮屏幕 | 通知亮屏（熄屏自动点亮） |
| 旋钮灵敏度值 | 1 档/步 · 2 档/步 · 3 档/步 |
| 待机屏半屏选择 | 待机屏·左 / 待机屏·右 |
| 试听按钮 | 试听 / 试震 |
| 区块标题 | 双语铭牌式 "NOTIFY · 通知"（英文大写拉字距 + 中文） |

## 图标系统

画法一处定义在 `tools/agent-bell-bridge/pack/icon_art.py`，三个用途同一套铃铛几何
（半圆罩 + 外张裙摆 + 收边横条 + 锤点，22 单位网格），所以看起来是一家人。

| 用途 | 画法 | 为什么 |
|---|---|---|
| 应用图标（Dock/任务栏/安装器）| 拉丝铝 squircle + 内凹深色屏 + 屏里印 TE 橙铃铛 | 它就是一小块 AgentBell 面板；深浅对比让它在 32px 也立得住 |
| 应用图标 16–24px | 只留橙铃铛，无机身 | 那种尺寸机身会糊成一团 |
| Windows 托盘 | 橙铃铛 + 右下状态点（绿=在线 红=离线）| 托盘允许彩色，状态点是有用信息 |
| macOS 菜单栏 | **模板图**（纯 alpha 单色），在线=实心铃铛 / 离线=空心铃铛 | 模板图由系统按菜单栏明暗自动上色，永远看得见；没有颜色可用，所以用形状区分状态（实测 22pt 下"加斜杠"会糊成一团） |

生成：`python pack/make_icon.py`（.ico，分尺寸画法）、`python pack/make_icns.py`（.icns）。
两个 PyInstaller spec 都要把 `icon_art.py` 带进 `datas`，否则打包版画不出托盘图标。

> macOS 陷阱：pystray 的 `_start_setup()` 在**后台线程**里设图标，而 AppKit 不允许
> 非主线程改 UI —— 菜单栏会只剩一个能点的空白位。必须自己在主线程
> `setImage_`（见 `bridge.py` 的 `_mac_apply_tray`），并给 `run_detached` 传空 setup
> 避免两边打架。

## 模式与底线（Operate）

表达永不遮蔽任务。正文对比度 ≥4.5:1；`:focus-visible` 橙框；`prefers-reduced-motion` 尊重；
中文文案说人话——控件名其动作，错误名其问题与恢复路径。
